# pdf2md 转换质量评估工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `pdf2md.py` 的 5 份已跑产物做"MD vs 程序化基线"的自动化评估，输出汇总 + 分页明细 + TOP-3 bad-case diff。

**Architecture:** 新增独立 `eval/` 包，旁路 `pdf2md.py`。两条流水线：纯文本（PyMuPDF 基线 + LCS 指标）和表格单元格（pdfplumber 基线 + multiset 指标）。逐页计算，micro-average 聚合。

**Tech Stack:** Python 3, pytest, PyMuPDF (已装), pdfplumber (新增), PyYAML (已装), 标准库 `difflib` / `csv` / `json` / `re` / `unicodedata`.

**Spec:** `docs/superpowers/specs/2026-04-17-pdf2md-eval-design.md`

---

## 文件总览

| 文件 | 职责 |
|---|---|
| `eval/__init__.py` | 空包标记 |
| `eval/normalize.py` | 文本归一化（NFKC、去零宽、压缩空白、英文小写）— 基线和 MD 共用 |
| `eval/baseline.py` | PyMuPDF 抽文本 + pdfplumber 抽表格 + 扫描件识别 |
| `eval/md_parser.py` | 逐页解析 `pages/*.md` 成 `(text, tables)` |
| `eval/metrics_text.py` | 字符级 LCS P/R/F1/edit_ratio + 词级（英文） |
| `eval/metrics_table.py` | 多表贪心 Jaccard 配对 + 单元格 multiset P/R/F1 |
| `eval/report.py` | 聚合 + summary.csv/md + per_page.csv + bad_cases.md + raw.json |
| `eval/run_eval.py` | 入口：读 config.yaml，orchestration，控制台进度 |
| `eval/config.yaml` | 5 份 PDF 的源路径 + output 路径（绝对路径，.gitignore） |
| `eval/tests/` | pytest 测试套件 |
| `.gitignore` | 追加 `eval_output/` 和 `eval/config.yaml` |

---

## Task 1: 项目脚手架与依赖

**Files:**
- Create: `eval/__init__.py`
- Create: `eval/tests/__init__.py`
- Create: `eval/config.yaml.example`
- Modify: `.gitignore`

- [ ] **Step 1: 安装 pdfplumber**

Run: `pip install pdfplumber`
Expected: 成功安装，`python3 -c "import pdfplumber; print(pdfplumber.__version__)"` 能打印版本号。

- [ ] **Step 2: 创建 eval 包结构**

创建 `eval/__init__.py`，内容为空。
创建 `eval/tests/__init__.py`，内容为空。

- [ ] **Step 3: 创建 config.yaml.example**

Create `eval/config.yaml.example`:

```yaml
# eval/config.yaml.example
# 复制为 config.yaml（不入库）后按实际路径填写
#
# 每份 PDF 一个条目：
#   name: 用于报告标题，也是 eval_output 子目录名
#   pdf:  源 PDF 绝对路径
#   md_dir: pdf2md 产物目录（包含 canonical.md, pages/, manifest.json）
#   lang: zh 或 en

items:
  - name: "天津真格"
    pdf: "/Users/aisenyc/Desktop/归档/【2024 年报】天津真格天弘资产管理合伙企业（有限合伙）2024年年报_三亚奥美信达投资中心（有限合伙）.pdf"
    md_dir: "/Users/aisenyc/Desktop/归档/output/【2024 年报】天津真格天弘资产管理合伙企业（有限合伙）2024年年报_三亚奥美信达投资中心（有限合伙）"
    lang: "zh"
  - name: "昆山峰瑞"
    pdf: "/Users/aisenyc/Desktop/归档/昆山峰瑞24年度投资报告.pdf"
    md_dir: "/Users/aisenyc/Desktop/归档/output/昆山峰瑞24年度投资报告"
    lang: "zh"
  - name: "广州越秀"
    pdf: "/Users/aisenyc/Desktop/归档/广州越秀.pdf"
    md_dir: "/Users/aisenyc/Desktop/归档/output/广州越秀"
    lang: "zh"
  - name: "苏州济峰"
    pdf: "/Users/aisenyc/Desktop/归档/【2024 年报】苏州济峰基金2024年度报告_Final.pdf"
    md_dir: "/Users/aisenyc/Desktop/归档/output/【2024 年报】苏州济峰基金2024年度报告_Final"
    lang: "zh"
  - name: "IDG"
    pdf: "/Users/aisenyc/Desktop/归档/【2024 年报】IDG China Capital Fund III LP_Annual Report_December 31, 2024 纯英文报告.pdf"
    md_dir: "/Users/aisenyc/Desktop/归档/output/【2024 年报】IDG China Capital Fund III LP_Annual Report_December 31, 2024 纯英文报告"
    lang: "en"

output_dir: "eval_output"
```

- [ ] **Step 4: 更新 .gitignore**

在 `.gitignore` 末尾追加：

```
eval_output/
eval/config.yaml
```

- [ ] **Step 5: 验证包可导入**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && python3 -c "import eval; import eval.tests"`
Expected: 无报错，无输出。

- [ ] **Step 6: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/__init__.py eval/tests/__init__.py eval/config.yaml.example .gitignore
git commit -m "chore(eval): scaffold eval package and config example"
```

---

## Task 2: 文本归一化模块

**Files:**
- Create: `eval/normalize.py`
- Create: `eval/tests/test_normalize.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_normalize.py`:

```python
from eval.normalize import normalize_text


def test_normalize_nfkc_fullwidth_to_halfwidth():
    assert normalize_text("ＡＢＣ１２３") == "abc123"


def test_normalize_removes_zero_width():
    assert normalize_text("hel\u200blo\ufefff") == "hellof"


def test_normalize_collapses_whitespace():
    assert normalize_text("a   b\n\n\nc\t\td") == "a b c d"


def test_normalize_lowercases_ascii():
    assert normalize_text("Hello World") == "hello world"


def test_normalize_preserves_cjk():
    assert normalize_text("本基金  成立于\u200b2020年") == "本基金 成立于2020年"


def test_normalize_empty_string():
    assert normalize_text("") == ""


def test_normalize_none_safe():
    assert normalize_text(None) == ""
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_normalize.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.normalize'`

- [ ] **Step 3: 最小实现**

Create `eval/normalize.py`:

```python
"""文本归一化——基线和 MD 解析共用同一份规则，否则归一化噪音会淹没真实差异。"""

from __future__ import annotations

import re
import unicodedata

_ZERO_WIDTH = re.compile(r"[\u200b-\u200f\ufeff\u2060]")
_WHITESPACE = re.compile(r"\s+")


def normalize_text(text: str | None) -> str:
    if not text:
        return ""
    text = unicodedata.normalize("NFKC", text)
    text = _ZERO_WIDTH.sub("", text)
    text = _WHITESPACE.sub(" ", text)
    text = text.strip()
    text = text.lower()
    return text
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_normalize.py -v`
Expected: 7 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/normalize.py eval/tests/test_normalize.py
git commit -m "feat(eval): add shared text normalization"
```

---

## Task 3: baseline.py — PyMuPDF 文本抽取 + 扫描页识别

**Files:**
- Create: `eval/baseline.py`
- Create: `eval/tests/test_baseline_text.py`
- Create: `eval/tests/fixtures/tiny.pdf`（通过脚本生成，见步骤 1）

- [ ] **Step 1: 生成测试用最小 PDF fixture**

Run:

```bash
cd /Users/aisenyc/Desktop/pdf-parser && mkdir -p eval/tests/fixtures && python3 <<'EOF'
import fitz
doc = fitz.open()
page = doc.new_page()
page.insert_text((72, 72), "Hello World")
page.insert_text((72, 120), "Second Line")
page2 = doc.new_page()
page2.insert_text((72, 72), "Page Two Content")
doc.save("eval/tests/fixtures/tiny.pdf")
doc.close()
print("ok")
EOF
```

Expected: 打印 `ok`，生成 `eval/tests/fixtures/tiny.pdf`（2 页）。

- [ ] **Step 2: 写失败测试**

Create `eval/tests/test_baseline_text.py`:

```python
from pathlib import Path

import pytest

from eval.baseline import extract_baseline_text, is_scanned_page

FIXTURE = Path(__file__).parent / "fixtures" / "tiny.pdf"


def test_extract_text_returns_one_string_per_page():
    texts = extract_baseline_text(FIXTURE)
    assert len(texts) == 2
    assert "hello world" in texts[0]
    assert "page two content" in texts[1]


def test_extract_text_applies_normalization():
    texts = extract_baseline_text(FIXTURE)
    # 归一化后应为小写、单空格
    assert texts[0] == texts[0].lower()
    assert "  " not in texts[0]


def test_is_scanned_page_detects_empty():
    assert is_scanned_page("") is True
    assert is_scanned_page("   ") is True
    assert is_scanned_page("short") is True  # <20 字符


def test_is_scanned_page_on_long_text():
    assert is_scanned_page("a" * 25) is False


def test_extract_text_missing_file_raises():
    with pytest.raises(FileNotFoundError):
        extract_baseline_text(Path("/tmp/nonexistent_xyz.pdf"))
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_baseline_text.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.baseline'`

- [ ] **Step 4: 最小实现**

Create `eval/baseline.py`:

```python
"""从源 PDF 抽取基线数据（文本 + 表格）。"""

from __future__ import annotations

import logging
from pathlib import Path

import fitz  # PyMuPDF

from eval.normalize import normalize_text

logger = logging.getLogger(__name__)

SCANNED_THRESHOLD = 20  # 归一化后文本长度小于此值，视为扫描页


def extract_baseline_text(pdf_path: Path) -> list[str]:
    """返回每页归一化后的纯文本，长度 == PDF 页数。"""
    if not Path(pdf_path).exists():
        raise FileNotFoundError(f"PDF not found: {pdf_path}")
    out: list[str] = []
    with fitz.open(pdf_path) as doc:
        for page in doc:
            try:
                raw = page.get_text("text")
            except Exception as e:  # noqa: BLE001
                logger.warning("PyMuPDF get_text failed on page %d: %s", page.number + 1, e)
                raw = ""
            out.append(normalize_text(raw))
    return out


def is_scanned_page(normalized_text: str) -> bool:
    """归一化后文本长度 < 阈值 → 扫描页（基线不可信）。"""
    return len(normalized_text.strip()) < SCANNED_THRESHOLD
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_baseline_text.py -v`
Expected: 5 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/baseline.py eval/tests/test_baseline_text.py eval/tests/fixtures/tiny.pdf
git commit -m "feat(eval): add PyMuPDF text baseline extraction"
```

---

## Task 4: baseline.py — pdfplumber 表格抽取

**Files:**
- Modify: `eval/baseline.py`
- Create: `eval/tests/test_baseline_tables.py`
- Create: `eval/tests/fixtures/tiny_with_table.pdf`

- [ ] **Step 1: 生成带表格的 fixture PDF**

Run:

```bash
cd /Users/aisenyc/Desktop/pdf-parser && python3 <<'EOF'
import fitz
doc = fitz.open()
page = doc.new_page()
page.insert_text((72, 72), "Header")
# 画一个简单 2x3 表格（矩形 + 文字）
rows = [("Name", "Age", "City"), ("Alice", "30", "NYC"), ("Bob", "25", "LA")]
x0, y0 = 72, 120
cw, rh = 80, 24
for r, row in enumerate(rows):
    for c, cell in enumerate(row):
        rect = fitz.Rect(x0 + c*cw, y0 + r*rh, x0 + (c+1)*cw, y0 + (r+1)*rh)
        page.draw_rect(rect, color=(0, 0, 0), width=0.5)
        page.insert_text((rect.x0 + 4, rect.y0 + 16), cell)
doc.save("eval/tests/fixtures/tiny_with_table.pdf")
doc.close()
print("ok")
EOF
```

Expected: 打印 `ok`。

- [ ] **Step 2: 写失败测试**

Create `eval/tests/test_baseline_tables.py`:

```python
from pathlib import Path

from eval.baseline import extract_baseline_tables

FIXTURE = Path(__file__).parent / "fixtures" / "tiny_with_table.pdf"
NO_TABLE = Path(__file__).parent / "fixtures" / "tiny.pdf"


def test_extract_tables_returns_one_list_per_page():
    tables = extract_baseline_tables(FIXTURE)
    assert len(tables) == 1  # 单页 PDF
    assert isinstance(tables[0], list)


def test_extract_tables_finds_table_with_cells():
    tables = extract_baseline_tables(FIXTURE)
    page_tables = tables[0]
    assert len(page_tables) >= 1
    # 扁平化所有单元格
    all_cells = [c for t in page_tables for row in t for c in row]
    assert "Alice" in all_cells
    assert "30" in all_cells
    assert "NYC" in all_cells


def test_extract_tables_normalizes_none_to_empty_string():
    tables = extract_baseline_tables(FIXTURE)
    for page in tables:
        for table in page:
            for row in table:
                for cell in row:
                    assert isinstance(cell, str)
                    assert cell is not None


def test_extract_tables_no_table_page_returns_empty_list():
    tables = extract_baseline_tables(NO_TABLE)
    # tiny.pdf 没表格，至少每页是空 list 不是 None
    assert len(tables) == 2
    for page in tables:
        assert isinstance(page, list)
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_baseline_tables.py -v`
Expected: FAIL with `ImportError: cannot import name 'extract_baseline_tables'`

- [ ] **Step 4: 最小实现**

Append to `eval/baseline.py`:

```python
import pdfplumber


def extract_baseline_tables(pdf_path: Path) -> list[list[list[list[str]]]]:
    """返回每页的表格列表。

    外层：页 → 中层：页内的表 → 内层：行 → 单元格。
    某页 pdfplumber 抛异常时，该页对应 `[]`（空 list）并记 warning。
    """
    if not Path(pdf_path).exists():
        raise FileNotFoundError(f"PDF not found: {pdf_path}")
    out: list[list[list[list[str]]]] = []
    with pdfplumber.open(pdf_path) as pdf:
        for i, page in enumerate(pdf.pages):
            try:
                raw_tables = page.extract_tables() or []
            except Exception as e:  # noqa: BLE001
                logger.warning("pdfplumber extract_tables failed on page %d: %s", i + 1, e)
                raw_tables = []
            page_tables: list[list[list[str]]] = []
            for t in raw_tables:
                normalized = [
                    [("" if cell is None else str(cell).strip()) for cell in row]
                    for row in t
                ]
                page_tables.append(normalized)
            out.append(page_tables)
    return out
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_baseline_tables.py -v`
Expected: 4 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/baseline.py eval/tests/test_baseline_tables.py eval/tests/fixtures/tiny_with_table.pdf
git commit -m "feat(eval): add pdfplumber table baseline extraction"
```

---

## Task 5: md_parser.py — 剥 Markdown 标记

**Files:**
- Create: `eval/md_parser.py`
- Create: `eval/tests/test_md_parser_text.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_md_parser_text.py`:

```python
from eval.md_parser import strip_markdown


def test_strip_headings():
    assert strip_markdown("# Title\n## Subtitle\ncontent") == "title subtitle content"


def test_strip_emphasis():
    assert strip_markdown("**bold** and *italic* text") == "bold and italic text"


def test_strip_code_fences():
    md = "before\n```python\nx = 1\n```\nafter"
    assert "```" not in strip_markdown(md)
    assert "python" not in strip_markdown(md)
    assert "x = 1" not in strip_markdown(md)


def test_strip_inline_code():
    assert strip_markdown("use `foo()` now") == "use now"


def test_strip_links_and_images():
    assert strip_markdown("see [here](http://x) now") == "see here now"
    assert strip_markdown("![alt](img.png) caption") == "caption"


def test_strip_list_markers():
    assert strip_markdown("- item a\n- item b\n1. numbered") == "item a item b numbered"


def test_strip_blockquotes():
    assert strip_markdown("> quoted\n> line two") == "quoted line two"


def test_strip_tables_removed():
    md = "before\n| a | b |\n|---|---|\n| 1 | 2 |\nafter"
    result = strip_markdown(md)
    assert "a" not in result.split() or "before after" in result
    # 表格内容不应出现在纯文本里（表格走另一条路径）
    assert "|" not in result
    assert "---" not in result


def test_strip_preserves_cjk():
    md = "# 标题\n**基金**成立于2020年"
    result = strip_markdown(md)
    assert "标题" in result
    assert "基金" in result
    assert "2020" in result


def test_strip_empty_string():
    assert strip_markdown("") == ""
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_md_parser_text.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.md_parser'`

- [ ] **Step 3: 最小实现**

Create `eval/md_parser.py`:

```python
"""从 pdf2md 产出的 pages/*.md 解析出纯文本和表格，供评估使用。"""

from __future__ import annotations

import re
from pathlib import Path

from eval.normalize import normalize_text

_CODE_FENCE = re.compile(r"```.*?```", re.DOTALL)
_INLINE_CODE = re.compile(r"`[^`]*`")
_IMAGE = re.compile(r"!\[[^\]]*\]\([^)]*\)")
_LINK = re.compile(r"\[([^\]]*)\]\([^)]*\)")
_TABLE_BLOCK = re.compile(
    r"(?:^\|[^\n]*\|\s*\n)+",  # 连续的 | ... | 行
    re.MULTILINE,
)
_HTML_TABLE = re.compile(r"<table\b.*?</table>", re.DOTALL | re.IGNORECASE)
_HEADING = re.compile(r"^#{1,6}\s+", re.MULTILINE)
_BLOCKQUOTE = re.compile(r"^>\s?", re.MULTILINE)
_LIST_MARKER = re.compile(r"^(\s*)(?:[-*+]|\d+\.)\s+", re.MULTILINE)
_EMPHASIS = re.compile(r"(\*\*|__|\*|_)(.+?)\1")
_HR = re.compile(r"^-{3,}$", re.MULTILINE)


def strip_markdown(md: str) -> str:
    """剥离 Markdown 标记返回归一化后的纯文本。表格块会被整块移除
    （表格走 parse_md_tables 那条路径），避免单元格内容在文本指标里重复计入。"""
    if not md:
        return ""
    text = md
    text = _CODE_FENCE.sub(" ", text)
    text = _HTML_TABLE.sub(" ", text)
    text = _TABLE_BLOCK.sub(" ", text)
    text = _IMAGE.sub(" ", text)
    text = _LINK.sub(r"\1", text)
    text = _INLINE_CODE.sub(" ", text)
    text = _HEADING.sub("", text)
    text = _BLOCKQUOTE.sub("", text)
    text = _LIST_MARKER.sub(r"\1", text)
    text = _EMPHASIS.sub(r"\2", text)
    text = _HR.sub(" ", text)
    return normalize_text(text)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_md_parser_text.py -v`
Expected: 10 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/md_parser.py eval/tests/test_md_parser_text.py
git commit -m "feat(eval): add markdown stripping to plain text"
```

---

## Task 6: md_parser.py — 解析 Markdown 表格

**Files:**
- Modify: `eval/md_parser.py`
- Create: `eval/tests/test_md_parser_tables.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_md_parser_tables.py`:

```python
from eval.md_parser import parse_md_tables


def test_parse_single_markdown_table():
    md = "| a | b |\n|---|---|\n| 1 | 2 |\n| 3 | 4 |"
    tables = parse_md_tables(md)
    assert len(tables) == 1
    assert tables[0] == [["a", "b"], ["1", "2"], ["3", "4"]]


def test_parse_two_markdown_tables():
    md = (
        "| a | b |\n|---|---|\n| 1 | 2 |\n\n"
        "text between\n\n"
        "| x | y |\n|---|---|\n| 9 | 8 |\n"
    )
    tables = parse_md_tables(md)
    assert len(tables) == 2
    assert tables[0][0] == ["a", "b"]
    assert tables[1][0] == ["x", "y"]


def test_parse_strips_cell_whitespace():
    md = "|  a  |  b  |\n|---|---|\n|   1   |   2   |"
    tables = parse_md_tables(md)
    assert tables[0] == [["a", "b"], ["1", "2"]]


def test_parse_handles_html_table():
    md = "<table><tr><td>a</td><td>b</td></tr><tr><td>1</td><td>2</td></tr></table>"
    tables = parse_md_tables(md)
    assert len(tables) == 1
    assert tables[0] == [["a", "b"], ["1", "2"]]


def test_parse_no_tables_returns_empty_list():
    assert parse_md_tables("just plain text") == []
    assert parse_md_tables("") == []


def test_parse_skips_separator_row():
    md = "| a | b |\n|---|---|\n| 1 | 2 |"
    tables = parse_md_tables(md)
    # 分隔行 |---|---| 不应出现在结果里
    for row in tables[0]:
        for cell in row:
            assert "---" not in cell
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_md_parser_tables.py -v`
Expected: FAIL with `ImportError: cannot import name 'parse_md_tables'`

- [ ] **Step 3: 最小实现**

Append to `eval/md_parser.py`:

```python
_MD_TABLE_ROW = re.compile(r"^\|(.+)\|\s*$")
_SEPARATOR_ROW = re.compile(r"^\s*[:\-| ]+\s*$")


def _parse_markdown_tables(md: str) -> list[list[list[str]]]:
    """连续的 `| ... |` 行构成一张表，跳过分隔行 `|---|---|`。"""
    tables: list[list[list[str]]] = []
    current: list[list[str]] = []
    for line in md.splitlines():
        m = _MD_TABLE_ROW.match(line)
        if m:
            inner = m.group(1)
            # 跳过分隔行
            if _SEPARATOR_ROW.match(inner.replace("|", "")):
                continue
            cells = [c.strip() for c in inner.split("|")]
            current.append(cells)
        else:
            if current:
                tables.append(current)
                current = []
    if current:
        tables.append(current)
    return tables


_HTML_TR = re.compile(r"<tr\b.*?</tr>", re.DOTALL | re.IGNORECASE)
_HTML_TD_TH = re.compile(r"<t[dh]\b[^>]*>(.*?)</t[dh]>", re.DOTALL | re.IGNORECASE)
_HTML_TAGS = re.compile(r"<[^>]+>")


def _parse_html_tables(md: str) -> list[list[list[str]]]:
    tables: list[list[list[str]]] = []
    for tbl in _HTML_TABLE.findall(md):
        rows: list[list[str]] = []
        for tr in _HTML_TR.findall(tbl):
            cells = [_HTML_TAGS.sub("", c).strip() for c in _HTML_TD_TH.findall(tr)]
            if cells:
                rows.append(cells)
        if rows:
            tables.append(rows)
    return tables


def parse_md_tables(md: str) -> list[list[list[str]]]:
    """从 MD 里抽出所有表格（Markdown pipe 表 + HTML table）。单元格已 strip。"""
    if not md:
        return []
    return _parse_html_tables(md) + _parse_markdown_tables(md)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_md_parser_tables.py -v`
Expected: 6 passed，且 Task 5 的测试仍然 pass（`pytest eval/tests/test_md_parser_text.py -v`）。

- [ ] **Step 5: 新增页级读取辅助函数**

Append to `eval/md_parser.py`:

```python
def read_page_md(md_dir: Path, page_number: int) -> str | None:
    """读取 pages/NNN.md 的原始内容。缺失返回 None。编码优先 utf-8，失败 fallback gbk。"""
    path = md_dir / "pages" / f"{page_number:03d}.md"
    if not path.exists():
        return None
    try:
        return path.read_text(encoding="utf-8")
    except UnicodeDecodeError:
        return path.read_text(encoding="gbk")


def parse_page(md_dir: Path, page_number: int) -> tuple[str, list[list[list[str]]]] | None:
    """返回 (纯文本, 表格列表)；若该页文件不存在，返回 None。"""
    raw = read_page_md(md_dir, page_number)
    if raw is None:
        return None
    return strip_markdown(raw), parse_md_tables(raw)
```

- [ ] **Step 6: 写页级读取的测试**

Append to `eval/tests/test_md_parser_tables.py`:

```python
from pathlib import Path
import tempfile

from eval.md_parser import parse_page, read_page_md


def test_read_page_md_returns_content(tmp_path):
    pages = tmp_path / "pages"
    pages.mkdir()
    (pages / "001.md").write_text("# Title\ncontent", encoding="utf-8")
    assert read_page_md(tmp_path, 1) == "# Title\ncontent"


def test_read_page_md_missing_returns_none(tmp_path):
    assert read_page_md(tmp_path, 999) is None


def test_parse_page_splits_text_and_tables(tmp_path):
    pages = tmp_path / "pages"
    pages.mkdir()
    (pages / "001.md").write_text(
        "# Title\nintro text\n\n| a | b |\n|---|---|\n| 1 | 2 |\n",
        encoding="utf-8",
    )
    result = parse_page(tmp_path, 1)
    assert result is not None
    text, tables = result
    assert "title" in text
    assert "intro text" in text
    assert len(tables) == 1
    assert tables[0][0] == ["a", "b"]
```

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_md_parser_tables.py -v`
Expected: 9 passed

- [ ] **Step 7: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/md_parser.py eval/tests/test_md_parser_tables.py
git commit -m "feat(eval): parse markdown + html tables and page-level reader"
```

---

## Task 7: metrics_text.py — 字符级 LCS P/R/F1 + 编辑距离

**Files:**
- Create: `eval/metrics_text.py`
- Create: `eval/tests/test_metrics_text.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_metrics_text.py`:

```python
import math

from eval.metrics_text import (
    char_metrics,
    edit_distance,
    edit_ratio,
)


def _approx(a, b, tol=1e-6):
    return math.isclose(a, b, rel_tol=0, abs_tol=tol)


def test_char_metrics_identical():
    m = char_metrics("hello world", "hello world")
    assert _approx(m["precision"], 1.0)
    assert _approx(m["recall"], 1.0)
    assert _approx(m["f1"], 1.0)


def test_char_metrics_subset_md_is_substring_of_baseline():
    # baseline 更长，md 是其真子序列 → recall<1, precision=1
    m = char_metrics(baseline="hello world", md="hello")
    assert _approx(m["precision"], 1.0)
    assert m["recall"] < 1.0
    assert m["recall"] > 0.0


def test_char_metrics_superset_md_has_extra():
    # md 多出一段 → precision<1, recall=1
    m = char_metrics(baseline="hello", md="hello world")
    assert m["precision"] < 1.0
    assert _approx(m["recall"], 1.0)


def test_char_metrics_completely_different():
    m = char_metrics("abcdef", "xyzuvw")
    assert m["f1"] < 0.2  # LCS 巧合匹配可能略大于 0


def test_char_metrics_empty_md_gives_zero_recall():
    m = char_metrics(baseline="hello", md="")
    assert _approx(m["precision"], 0.0)
    assert _approx(m["recall"], 0.0)
    assert _approx(m["f1"], 0.0)


def test_char_metrics_both_empty_returns_skipped():
    m = char_metrics(baseline="", md="")
    assert m.get("skipped") is True


def test_edit_distance_identical():
    assert edit_distance("abc", "abc") == 0


def test_edit_distance_insertions():
    assert edit_distance("abc", "abxc") == 1


def test_edit_distance_deletions_and_substitutions():
    assert edit_distance("kitten", "sitting") == 3


def test_edit_ratio_normalized_by_baseline():
    # 完全不同长度 3 → 3/3 = 1.0
    assert _approx(edit_ratio("abc", "xyz"), 1.0)
    # 相同 → 0
    assert _approx(edit_ratio("abc", "abc"), 0.0)


def test_edit_ratio_empty_baseline_returns_zero():
    # baseline 为空时，约定返回 0（扫描页场景）
    assert edit_ratio("", "anything") == 0.0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_text.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.metrics_text'`

- [ ] **Step 3: 最小实现**

Create `eval/metrics_text.py`:

```python
"""文本指标：字符级 LCS-based P/R/F1 + 编辑距离。

字符级比词级更适合中文；英文另外由 `word_metrics` 补一组词级指标。
"""

from __future__ import annotations

from difflib import SequenceMatcher
from typing import Any


def _lcs_length(a: str, b: str) -> int:
    """LCS 长度 = SequenceMatcher 所有匹配块 size 之和。"""
    matcher = SequenceMatcher(a=a, b=b, autojunk=False)
    return sum(block.size for block in matcher.get_matching_blocks())


def char_metrics(baseline: str, md: str) -> dict[str, Any]:
    """基于字符级 LCS 的 precision / recall / f1。

    两边都为空 → {"skipped": True}
    md 为空 → P=R=F1=0
    baseline 为空而 md 非空 → P=R=F1=0（全是多出来的）
    """
    if not baseline and not md:
        return {"skipped": True}
    if not md:
        return {"precision": 0.0, "recall": 0.0, "f1": 0.0, "skipped": False}
    if not baseline:
        return {"precision": 0.0, "recall": 0.0, "f1": 0.0, "skipped": False}
    lcs = _lcs_length(baseline, md)
    precision = lcs / len(md)
    recall = lcs / len(baseline)
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {"precision": precision, "recall": recall, "f1": f1, "skipped": False}


def edit_distance(a: str, b: str) -> int:
    """Levenshtein 编辑距离，滚动数组 DP。字符级，短文本足够快。"""
    if a == b:
        return 0
    if not a:
        return len(b)
    if not b:
        return len(a)
    prev = list(range(len(b) + 1))
    for i, ca in enumerate(a, 1):
        curr = [i] + [0] * len(b)
        for j, cb in enumerate(b, 1):
            cost = 0 if ca == cb else 1
            curr[j] = min(
                curr[j - 1] + 1,       # insertion
                prev[j] + 1,           # deletion
                prev[j - 1] + cost,    # substitution
            )
        prev = curr
    return prev[-1]


def edit_ratio(baseline: str, md: str) -> float:
    """edit_distance / len(baseline)。baseline 为空时返回 0.0。"""
    if not baseline:
        return 0.0
    return edit_distance(baseline, md) / len(baseline)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_text.py -v`
Expected: 11 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/metrics_text.py eval/tests/test_metrics_text.py
git commit -m "feat(eval): add char-level LCS text metrics + edit distance"
```

---

## Task 8: metrics_text.py — 词级指标（英文 PDF 用）

**Files:**
- Modify: `eval/metrics_text.py`
- Modify: `eval/tests/test_metrics_text.py`

- [ ] **Step 1: 写失败测试**

Append to `eval/tests/test_metrics_text.py`:

```python
from eval.metrics_text import word_metrics


def test_word_metrics_identical():
    m = word_metrics("hello world foo bar", "hello world foo bar")
    assert _approx(m["precision"], 1.0)
    assert _approx(m["recall"], 1.0)
    assert _approx(m["f1"], 1.0)


def test_word_metrics_whitespace_tokenized():
    # 多空白、换行都算分隔符
    m = word_metrics("a b c", "a  b\nc")
    assert _approx(m["f1"], 1.0)


def test_word_metrics_partial_overlap():
    m = word_metrics(baseline="the quick brown fox", md="the lazy fox")
    assert 0 < m["precision"] < 1
    assert 0 < m["recall"] < 1


def test_word_metrics_empty_both_skipped():
    m = word_metrics("", "")
    assert m.get("skipped") is True
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_text.py::test_word_metrics_identical -v`
Expected: FAIL with `ImportError: cannot import name 'word_metrics'`

- [ ] **Step 3: 最小实现**

Append to `eval/metrics_text.py`:

```python
def word_metrics(baseline: str, md: str) -> dict[str, Any]:
    """英文按空白切词后跑 LCS。中文不建议用（分词会引入额外噪音）。"""
    b_tokens = baseline.split()
    m_tokens = md.split()
    if not b_tokens and not m_tokens:
        return {"skipped": True}
    if not m_tokens or not b_tokens:
        return {"precision": 0.0, "recall": 0.0, "f1": 0.0, "skipped": False}
    matcher = SequenceMatcher(a=b_tokens, b=m_tokens, autojunk=False)
    lcs = sum(block.size for block in matcher.get_matching_blocks())
    precision = lcs / len(m_tokens)
    recall = lcs / len(b_tokens)
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {"precision": precision, "recall": recall, "f1": f1, "skipped": False}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_text.py -v`
Expected: 15 passed（11 旧 + 4 新）

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/metrics_text.py eval/tests/test_metrics_text.py
git commit -m "feat(eval): add word-level text metrics for English"
```

---

## Task 9: metrics_table.py — 单表单元格 multiset P/R/F1

**Files:**
- Create: `eval/metrics_table.py`
- Create: `eval/tests/test_metrics_table.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_metrics_table.py`:

```python
import math

from eval.metrics_table import (
    cell_multiset,
    pair_metrics,
)


def _approx(a, b, tol=1e-6):
    return math.isclose(a, b, rel_tol=0, abs_tol=tol)


def test_cell_multiset_flattens_2d_array():
    t = [["a", "b"], ["c", "a"]]
    ms = cell_multiset(t)
    assert ms["a"] == 2
    assert ms["b"] == 1
    assert ms["c"] == 1


def test_cell_multiset_skips_empty_cells():
    t = [["a", ""], ["", "b"]]
    ms = cell_multiset(t)
    assert "" not in ms
    assert ms["a"] == 1
    assert ms["b"] == 1


def test_pair_metrics_identical_tables():
    t = [["a", "b"], ["1", "2"]]
    m = pair_metrics(t, t)
    assert _approx(m["precision"], 1.0)
    assert _approx(m["recall"], 1.0)
    assert _approx(m["f1"], 1.0)


def test_pair_metrics_md_missing_row():
    baseline = [["a", "b"], ["1", "2"], ["3", "4"]]
    md = [["a", "b"], ["1", "2"]]
    m = pair_metrics(md=md, baseline=baseline)
    # md 里 4 个单元格全对 → P=1; baseline 里 6 个单元格命中 4 → R=4/6
    assert _approx(m["precision"], 1.0)
    assert _approx(m["recall"], 4 / 6)


def test_pair_metrics_md_extra_column():
    baseline = [["a", "b"], ["1", "2"]]
    md = [["a", "b", "EXTRA"], ["1", "2", "EXTRA2"]]
    m = pair_metrics(md=md, baseline=baseline)
    # md 6 单元格命中 4 → P=4/6; baseline 4 单元格全命中 → R=1
    assert _approx(m["precision"], 4 / 6)
    assert _approx(m["recall"], 1.0)


def test_pair_metrics_disjoint_tables():
    m = pair_metrics(md=[["x"]], baseline=[["y"]])
    assert _approx(m["precision"], 0.0)
    assert _approx(m["recall"], 0.0)
    assert _approx(m["f1"], 0.0)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_table.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.metrics_table'`

- [ ] **Step 3: 最小实现**

Create `eval/metrics_table.py`:

```python
"""表格指标：多表贪心 Jaccard 配对 + 单元格 multiset P/R/F1。

不按行列位置对齐，而是把单元格拉平成 multiset 后求交集。代价是放过"行错位但内容全在"
的情况，但能容忍 pdfplumber 常见的抽多/抽少一行表头；这对评估 pdf2md 还原度更实用。
"""

from __future__ import annotations

from collections import Counter
from typing import Any

Table = list[list[str]]


def cell_multiset(table: Table) -> Counter:
    """把 2D 表拉平成 {单元格值: 出现次数} 的 Counter；空字符串跳过。"""
    c: Counter = Counter()
    for row in table:
        for cell in row:
            if cell:
                c[cell] += 1
    return c


def _prf_from_counts(tp: int, fp: int, fn: int) -> dict[str, float]:
    precision = tp / (tp + fp) if (tp + fp) > 0 else 0.0
    recall = tp / (tp + fn) if (tp + fn) > 0 else 0.0
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {"precision": precision, "recall": recall, "f1": f1}


def pair_metrics(md: Table, baseline: Table) -> dict[str, Any]:
    """对一对已配对的 (md_table, baseline_table) 计算 P/R/F1。"""
    ms_md = cell_multiset(md)
    ms_base = cell_multiset(baseline)
    total_md = sum(ms_md.values())
    total_base = sum(ms_base.values())
    tp = sum((ms_md & ms_base).values())
    fp = total_md - tp
    fn = total_base - tp
    result = _prf_from_counts(tp, fp, fn)
    result["tp"] = tp
    result["fp"] = fp
    result["fn"] = fn
    return result
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_table.py -v`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/metrics_table.py eval/tests/test_metrics_table.py
git commit -m "feat(eval): add single-table cell multiset metrics"
```

---

## Task 10: metrics_table.py — 多表贪心 Jaccard 配对 + 页级聚合

**Files:**
- Modify: `eval/metrics_table.py`
- Modify: `eval/tests/test_metrics_table.py`

- [ ] **Step 1: 写失败测试**

Append to `eval/tests/test_metrics_table.py`:

```python
from eval.metrics_table import page_table_metrics


def test_page_metrics_no_tables_returns_skipped():
    m = page_table_metrics(md_tables=[], baseline_tables=[])
    assert m.get("skipped") is True


def test_page_metrics_baseline_missing_returns_skipped():
    # 基线抽不到表（扫描件/pdfplumber 挂），即使 md 有表也跳过（基线不可信）
    m = page_table_metrics(md_tables=[[["a", "b"]]], baseline_tables=[])
    assert m.get("skipped") is True
    assert m.get("reason") == "baseline_missing"


def test_page_metrics_md_missing_all_tables_fn():
    # md 没表但基线有 → 全是 FN
    baseline = [[["a", "b"], ["1", "2"]]]
    m = page_table_metrics(md_tables=[], baseline_tables=baseline)
    assert m.get("skipped") is False
    assert _approx(m["precision"], 0.0)
    assert _approx(m["recall"], 0.0)
    assert m["tables_matched"] == 0


def test_page_metrics_one_to_one_match():
    t = [["a", "b"], ["1", "2"]]
    m = page_table_metrics(md_tables=[t], baseline_tables=[t])
    assert _approx(m["f1"], 1.0)
    assert m["tables_matched"] == 1


def test_page_metrics_greedy_pairing_by_jaccard():
    # 基线 2 表、md 2 表，内容相似度明确 → 期望正确配对
    base1 = [["x", "y"], ["1", "2"]]
    base2 = [["a", "b"], ["9", "8"]]
    md1_looks_like_base2 = [["a", "b"], ["9", "8"]]
    md2_looks_like_base1 = [["x", "y"], ["1", "2"]]
    m = page_table_metrics(
        md_tables=[md1_looks_like_base2, md2_looks_like_base1],
        baseline_tables=[base1, base2],
    )
    assert _approx(m["f1"], 1.0)
    assert m["tables_matched"] == 2


def test_page_metrics_unpaired_md_table_counts_as_fp():
    base = [["a", "b"]]
    md_matched = [["a", "b"]]
    md_unmatched = [["zzz", "qqq"]]  # 与 base 完全不相交
    m = page_table_metrics(
        md_tables=[md_matched, md_unmatched],
        baseline_tables=[base],
    )
    # md 一共 4 个非空单元格，配对那张贡献 2 TP，未配对那张 2 FP
    # base 2 个全 TP → R=1, P=2/4=0.5
    assert _approx(m["precision"], 0.5)
    assert _approx(m["recall"], 1.0)
    assert m["tables_matched"] == 1
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_table.py -v`
Expected: 新增 6 个测试 FAIL with `ImportError: cannot import name 'page_table_metrics'`

- [ ] **Step 3: 最小实现**

Append to `eval/metrics_table.py`:

```python
JACCARD_THRESHOLD = 0.1


def _jaccard(a: Table, b: Table) -> float:
    sa = set(cell_multiset(a).keys())
    sb = set(cell_multiset(b).keys())
    if not sa and not sb:
        return 0.0
    inter = len(sa & sb)
    union = len(sa | sb)
    return inter / union if union > 0 else 0.0


def page_table_metrics(
    md_tables: list[Table],
    baseline_tables: list[Table],
) -> dict[str, Any]:
    """页级表格指标：贪心配对 + TP/FP/FN 累加。

    约定：
    - 基线无表 → skipped=True, reason=baseline_missing（基线不可信，不评）
    - 基线有表但 md 无表 → P=R=F1=0，不 skip（是真正的 miss）
    - 基线和 md 都无表 → skipped=True, reason=no_tables
    """
    if not baseline_tables and not md_tables:
        return {"skipped": True, "reason": "no_tables"}
    if not baseline_tables:
        return {"skipped": True, "reason": "baseline_missing"}

    # 构造 (md_idx, base_idx, jaccard) 候选并按相似度降序
    candidates: list[tuple[int, int, float]] = []
    for i, t_md in enumerate(md_tables):
        for j, t_base in enumerate(baseline_tables):
            s = _jaccard(t_md, t_base)
            if s >= JACCARD_THRESHOLD:
                candidates.append((i, j, s))
    candidates.sort(key=lambda x: x[2], reverse=True)

    used_md: set[int] = set()
    used_base: set[int] = set()
    pairs: list[tuple[int, int]] = []
    for i, j, _ in candidates:
        if i in used_md or j in used_base:
            continue
        pairs.append((i, j))
        used_md.add(i)
        used_base.add(j)

    tp = fp = fn = 0
    for i, j in pairs:
        r = pair_metrics(md_tables[i], baseline_tables[j])
        tp += r["tp"]
        fp += r["fp"]
        fn += r["fn"]

    # 未配对 md 表 → 所有单元格算 FP
    for i, t in enumerate(md_tables):
        if i not in used_md:
            fp += sum(cell_multiset(t).values())
    # 未配对 baseline 表 → 所有单元格算 FN
    for j, t in enumerate(baseline_tables):
        if j not in used_base:
            fn += sum(cell_multiset(t).values())

    result = _prf_from_counts(tp, fp, fn)
    result["tp"] = tp
    result["fp"] = fp
    result["fn"] = fn
    result["tables_matched"] = len(pairs)
    result["tables_total_baseline"] = len(baseline_tables)
    result["skipped"] = False
    return result
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_metrics_table.py -v`
Expected: 12 passed（6 旧 + 6 新）

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/metrics_table.py eval/tests/test_metrics_table.py
git commit -m "feat(eval): add page-level table metrics with greedy Jaccard pairing"
```

---

## Task 11: report.py — 页级评估函数（把所有指标串起来）

**Files:**
- Create: `eval/report.py`
- Create: `eval/tests/test_report_page_eval.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_report_page_eval.py`:

```python
from eval.report import evaluate_page


def test_evaluate_page_both_text_and_tables():
    r = evaluate_page(
        baseline_text="hello world this is a test",
        md_text="hello world this is a test",
        baseline_tables=[[["a", "b"], ["1", "2"]]],
        md_tables=[[["a", "b"], ["1", "2"]]],
        lang="en",
    )
    assert r["status"] == "ok"
    assert r["text"]["f1"] == 1.0
    assert r["table"]["f1"] == 1.0
    # 英文 PDF 多一组词级指标
    assert "word" in r
    assert r["word"]["f1"] == 1.0


def test_evaluate_page_scanned():
    r = evaluate_page(
        baseline_text="",  # 基线为空 → 扫描件
        md_text="ocr reconstructed text here long enough",
        baseline_tables=[],
        md_tables=[],
        lang="zh",
    )
    assert r["status"] == "scanned"
    assert r["text"].get("skipped") is True


def test_evaluate_page_table_baseline_missing():
    r = evaluate_page(
        baseline_text="some baseline text long enough",
        md_text="some baseline text long enough",
        baseline_tables=[],   # 基线无表
        md_tables=[[["x", "y"]]],  # MD 有表
        lang="zh",
    )
    assert r["status"] == "table_missing"
    # 文本指标照算
    assert r["text"].get("skipped") is False
    assert r["table"].get("skipped") is True


def test_evaluate_page_chinese_no_word_metrics():
    r = evaluate_page(
        baseline_text="中文内容测试内容",
        md_text="中文内容测试内容",
        baseline_tables=[],
        md_tables=[],
        lang="zh",
    )
    assert "word" not in r or r.get("word") is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_page_eval.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'eval.report'`

- [ ] **Step 3: 最小实现**

Create `eval/report.py`:

```python
"""报告生成：页级评估、聚合、多种格式输出。"""

from __future__ import annotations

import csv
import json
from dataclasses import asdict, dataclass, field
from difflib import unified_diff
from pathlib import Path
from typing import Any

from eval.baseline import is_scanned_page
from eval.metrics_table import cell_multiset, page_table_metrics
from eval.metrics_text import char_metrics, edit_ratio, word_metrics


def evaluate_page(
    baseline_text: str,
    md_text: str,
    baseline_tables: list[list[list[str]]],
    md_tables: list[list[list[str]]],
    lang: str,
) -> dict[str, Any]:
    """给单页算出所有指标。`status` 字段：ok / scanned / table_missing / page_missing。"""
    scanned = is_scanned_page(baseline_text)
    table_base_empty = len(baseline_tables) == 0
    table_md_nonempty = len(md_tables) > 0

    status = "ok"
    if scanned:
        status = "scanned"
    elif table_base_empty and table_md_nonempty:
        status = "table_missing"

    if scanned:
        text_m = {"skipped": True, "reason": "scanned"}
        edit = None
    else:
        text_m = char_metrics(baseline_text, md_text)
        edit = edit_ratio(baseline_text, md_text)

    table_m = page_table_metrics(md_tables=md_tables, baseline_tables=baseline_tables)

    result: dict[str, Any] = {
        "status": status,
        "baseline_text_len": len(baseline_text),
        "md_text_len": len(md_text),
        "text": text_m,
        "text_edit_ratio": edit,
        "baseline_tables": len(baseline_tables),
        "md_tables": len(md_tables),
        "table": table_m,
    }
    if lang == "en" and not scanned:
        result["word"] = word_metrics(baseline_text, md_text)
    return result
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_page_eval.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/report.py eval/tests/test_report_page_eval.py
git commit -m "feat(eval): add per-page evaluation aggregator"
```

---

## Task 12: report.py — PDF 级聚合（micro-average）

**Files:**
- Modify: `eval/report.py`
- Create: `eval/tests/test_report_aggregate.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_report_aggregate.py`:

```python
import math

from eval.report import aggregate_pdf


def _approx(a, b, tol=1e-6):
    return math.isclose(a, b, rel_tol=0, abs_tol=tol)


def _ok_page(text_tp, text_fp, text_fn, table_tp=0, table_fp=0, table_fn=0, lang="zh"):
    """构造一个 status=ok 的 page-level result，直接提供 TP/FP/FN 便于测 micro-average。"""
    return {
        "status": "ok",
        "baseline_text_len": text_tp + text_fn,
        "md_text_len": text_tp + text_fp,
        "text": {
            "skipped": False,
            "_tp": text_tp, "_fp": text_fp, "_fn": text_fn,
        },
        "text_edit_ratio": 0.0,
        "baseline_tables": 1 if table_tp + table_fn > 0 else 0,
        "md_tables": 1 if table_tp + table_fp > 0 else 0,
        "table": {
            "skipped": (table_tp + table_fp + table_fn) == 0,
            "tp": table_tp, "fp": table_fp, "fn": table_fn,
            "tables_matched": 1 if table_tp > 0 else 0,
            "tables_total_baseline": 1 if table_tp + table_fn > 0 else 0,
        },
    }


def test_aggregate_counts_pages_by_status():
    pages = [
        {"status": "ok", "text": {"skipped": False}, "table": {"skipped": True}, "baseline_text_len": 10, "md_text_len": 10, "text_edit_ratio": 0.0, "baseline_tables": 0, "md_tables": 0},
        {"status": "scanned", "text": {"skipped": True}, "table": {"skipped": True}, "baseline_text_len": 0, "md_text_len": 5, "text_edit_ratio": None, "baseline_tables": 0, "md_tables": 0},
        {"status": "table_missing", "text": {"skipped": False}, "table": {"skipped": True}, "baseline_text_len": 20, "md_text_len": 20, "text_edit_ratio": 0.0, "baseline_tables": 0, "md_tables": 1},
    ]
    agg = aggregate_pdf(pages, lang="zh")
    assert agg["pages_total"] == 3
    assert agg["pages_scanned"] == 1
    assert agg["pages_table_missing"] == 1


def test_aggregate_text_micro_average():
    # 2 页：(TP=8, FP=2, FN=2) + (TP=6, FP=4, FN=4) → micro: P=14/20=0.7, R=14/20=0.7
    pages = [
        {"status": "ok", "text": {"skipped": False}, "table": {"skipped": True}, "baseline_text_len": 10, "md_text_len": 10, "text_edit_ratio": 0.2, "baseline_tables": 0, "md_tables": 0, "_text_tp": 8, "_text_fp": 2, "_text_fn": 2},
        {"status": "ok", "text": {"skipped": False}, "table": {"skipped": True}, "baseline_text_len": 10, "md_text_len": 10, "text_edit_ratio": 0.4, "baseline_tables": 0, "md_tables": 0, "_text_tp": 6, "_text_fp": 4, "_text_fn": 4},
    ]
    agg = aggregate_pdf(pages, lang="zh")
    assert _approx(agg["text_P"], 0.7)
    assert _approx(agg["text_R"], 0.7)


def test_aggregate_table_micro_average():
    pages = [
        {"status": "ok", "text": {"skipped": True}, "table": {"skipped": False, "tp": 5, "fp": 1, "fn": 2, "tables_matched": 1, "tables_total_baseline": 1}, "baseline_text_len": 0, "md_text_len": 0, "text_edit_ratio": None, "baseline_tables": 1, "md_tables": 1},
        {"status": "ok", "text": {"skipped": True}, "table": {"skipped": False, "tp": 3, "fp": 2, "fn": 1, "tables_matched": 1, "tables_total_baseline": 2}, "baseline_text_len": 0, "md_text_len": 0, "text_edit_ratio": None, "baseline_tables": 2, "md_tables": 1},
    ]
    agg = aggregate_pdf(pages, lang="zh")
    # TP=8, FP=3, FN=3 → P=8/11, R=8/11
    assert _approx(agg["table_P"], 8 / 11)
    assert _approx(agg["table_R"], 8 / 11)
    assert agg["tables_total"] == 3
    assert agg["tables_matched"] == 2
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_aggregate.py -v`
Expected: FAIL with `ImportError: cannot import name 'aggregate_pdf'`

- [ ] **Step 3: 修改 `char_metrics` 返回 TP/FP/FN**

Modify `char_metrics` in `eval/metrics_text.py` — replace the body after the `if` guards with:

```python
    lcs = _lcs_length(baseline, md)
    precision = lcs / len(md)
    recall = lcs / len(baseline)
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {
        "precision": precision,
        "recall": recall,
        "f1": f1,
        "skipped": False,
        "_tp": lcs,
        "_fp": len(md) - lcs,
        "_fn": len(baseline) - lcs,
    }
```

（Task 7 的测试只断言 `precision/recall/f1/skipped` 三个字段，加 `_tp/_fp/_fn` 不破坏测试。）

- [ ] **Step 4: 修改 `evaluate_page` 透传 TP/FP/FN，实现 `aggregate_pdf`**

Modify `evaluate_page` in `eval/report.py`：在 `result` dict 构造之后，追加透传语句：

```python
    result["_text_tp"] = text_m.get("_tp", 0)
    result["_text_fp"] = text_m.get("_fp", 0)
    result["_text_fn"] = text_m.get("_fn", 0)
```

Then append `aggregate_pdf` at the bottom of `eval/report.py`:

```python
def aggregate_pdf(pages: list[dict[str, Any]], lang: str) -> dict[str, Any]:
    """对一份 PDF 的页级结果做 micro-average 聚合。"""
    pages_total = len(pages)
    pages_scanned = sum(1 for p in pages if p["status"] == "scanned")
    pages_table_missing = sum(1 for p in pages if p["status"] == "table_missing")
    pages_missing = sum(1 for p in pages if p["status"] == "page_missing")

    text_tp = sum(p.get("_text_tp", 0) for p in pages if not p["text"].get("skipped"))
    text_fp = sum(p.get("_text_fp", 0) for p in pages if not p["text"].get("skipped"))
    text_fn = sum(p.get("_text_fn", 0) for p in pages if not p["text"].get("skipped"))
    text_P = text_tp / (text_tp + text_fp) if (text_tp + text_fp) > 0 else 0.0
    text_R = text_tp / (text_tp + text_fn) if (text_tp + text_fn) > 0 else 0.0
    text_F1 = 2 * text_P * text_R / (text_P + text_R) if (text_P + text_R) > 0 else 0.0

    tp_t = sum(p["table"].get("tp", 0) for p in pages if not p["table"].get("skipped"))
    fp_t = sum(p["table"].get("fp", 0) for p in pages if not p["table"].get("skipped"))
    fn_t = sum(p["table"].get("fn", 0) for p in pages if not p["table"].get("skipped"))
    table_P = tp_t / (tp_t + fp_t) if (tp_t + fp_t) > 0 else 0.0
    table_R = tp_t / (tp_t + fn_t) if (tp_t + fn_t) > 0 else 0.0
    table_F1 = 2 * table_P * table_R / (table_P + table_R) if (table_P + table_R) > 0 else 0.0

    tables_total = sum(p["table"].get("tables_total_baseline", 0) for p in pages)
    tables_matched = sum(p["table"].get("tables_matched", 0) for p in pages)

    edit_values = [p["text_edit_ratio"] for p in pages if p.get("text_edit_ratio") is not None]
    text_edit_ratio = sum(edit_values) / len(edit_values) if edit_values else 0.0

    agg: dict[str, Any] = {
        "lang": lang,
        "pages_total": pages_total,
        "pages_scanned": pages_scanned,
        "pages_table_missing": pages_table_missing,
        "pages_missing": pages_missing,
        "text_P": text_P,
        "text_R": text_R,
        "text_F1": text_F1,
        "text_edit_ratio": text_edit_ratio,
        "table_P": table_P,
        "table_R": table_R,
        "table_F1": table_F1,
        "tables_total": tables_total,
        "tables_matched": tables_matched,
    }

    if lang == "en":
        w_tp = sum(p.get("word", {}).get("_tp", 0) for p in pages if p.get("word") and not p["word"].get("skipped"))
        w_fp = sum(p.get("word", {}).get("_fp", 0) for p in pages if p.get("word") and not p["word"].get("skipped"))
        w_fn = sum(p.get("word", {}).get("_fn", 0) for p in pages if p.get("word") and not p["word"].get("skipped"))
        agg["text_P_word"] = w_tp / (w_tp + w_fp) if (w_tp + w_fp) > 0 else 0.0
        agg["text_R_word"] = w_tp / (w_tp + w_fn) if (w_tp + w_fn) > 0 else 0.0
        agg["text_F1_word"] = (
            2 * agg["text_P_word"] * agg["text_R_word"] / (agg["text_P_word"] + agg["text_R_word"])
            if (agg["text_P_word"] + agg["text_R_word"]) > 0 else 0.0
        )
    else:
        agg["text_P_word"] = agg["text_R_word"] = agg["text_F1_word"] = None

    return agg
```

- [ ] **Step 5: 调整 `word_metrics` 返回 TP/FP/FN（用于英文聚合）**

Modify `word_metrics` in `eval/metrics_text.py` — replace the final `return` block:

```python
    matcher = SequenceMatcher(a=b_tokens, b=m_tokens, autojunk=False)
    lcs = sum(block.size for block in matcher.get_matching_blocks())
    precision = lcs / len(m_tokens)
    recall = lcs / len(b_tokens)
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {"precision": precision, "recall": recall, "f1": f1, "skipped": False}
```

with:

```python
    matcher = SequenceMatcher(a=b_tokens, b=m_tokens, autojunk=False)
    lcs = sum(block.size for block in matcher.get_matching_blocks())
    precision = lcs / len(m_tokens)
    recall = lcs / len(b_tokens)
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0.0
    return {
        "precision": precision,
        "recall": recall,
        "f1": f1,
        "skipped": False,
        "_tp": lcs,
        "_fp": len(m_tokens) - lcs,
        "_fn": len(b_tokens) - lcs,
    }
```

- [ ] **Step 6: 运行所有测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/ -v`
Expected: 全部 passed（包括之前的测试不受影响）。

- [ ] **Step 7: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/report.py eval/metrics_text.py eval/tests/test_report_aggregate.py
git commit -m "feat(eval): add per-PDF micro-average aggregation"
```

---

## Task 13: report.py — 写 summary.csv / summary.md / per_page.csv / raw.json

**Files:**
- Modify: `eval/report.py`
- Create: `eval/tests/test_report_output.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_report_output.py`:

```python
import csv
import json
from pathlib import Path

from eval.report import write_pdf_report, write_summary


def _fake_pdf_result(name, lang="zh"):
    return {
        "name": name,
        "lang": lang,
        "agg": {
            "lang": lang,
            "pages_total": 3,
            "pages_scanned": 0,
            "pages_table_missing": 0,
            "pages_missing": 0,
            "text_P": 0.9, "text_R": 0.85, "text_F1": 0.87,
            "text_edit_ratio": 0.1,
            "table_P": 0.8, "table_R": 0.7, "table_F1": 0.75,
            "tables_total": 5, "tables_matched": 4,
            "text_P_word": None, "text_R_word": None, "text_F1_word": None,
        },
        "pages": [
            {"page": 1, "status": "ok", "baseline_text_len": 100, "md_text_len": 98,
             "text": {"precision": 0.9, "recall": 0.9, "f1": 0.9, "skipped": False},
             "text_edit_ratio": 0.05, "baseline_tables": 1, "md_tables": 1,
             "table": {"precision": 0.8, "recall": 0.8, "f1": 0.8, "skipped": False},
             "md_method": "native"},
        ],
    }


def test_write_pdf_report_creates_files(tmp_path):
    result = _fake_pdf_result("sample")
    write_pdf_report(result, tmp_path)
    sub = tmp_path / "sample"
    assert (sub / "per_page.csv").exists()
    assert (sub / "raw.json").exists()
    # per_page 里能读到 page=1
    rows = list(csv.DictReader((sub / "per_page.csv").open(encoding="utf-8")))
    assert rows[0]["page"] == "1"
    assert rows[0]["status"] == "ok"
    # raw.json 能解析
    data = json.loads((sub / "raw.json").read_text(encoding="utf-8"))
    assert data["name"] == "sample"
    assert data["agg"]["pages_total"] == 3


def test_write_summary_writes_csv_and_md(tmp_path):
    results = [_fake_pdf_result("a"), _fake_pdf_result("b", lang="en")]
    # 英文那份带词级指标
    results[1]["agg"]["text_P_word"] = 0.95
    results[1]["agg"]["text_R_word"] = 0.9
    results[1]["agg"]["text_F1_word"] = 0.92
    write_summary(results, tmp_path)
    assert (tmp_path / "summary.csv").exists()
    assert (tmp_path / "summary.md").exists()
    # summary.csv 有 2 行数据
    rows = list(csv.DictReader((tmp_path / "summary.csv").open(encoding="utf-8")))
    assert len(rows) == 2
    # 中文行 text_P_word 为空字符串
    zh = [r for r in rows if r["pdf"] == "a"][0]
    assert zh["text_P_word"] == ""
    # summary.md 包含 TL;DR 段
    md_text = (tmp_path / "summary.md").read_text(encoding="utf-8")
    assert "TL;DR" in md_text or "tl;dr" in md_text.lower()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_output.py -v`
Expected: FAIL with `ImportError: cannot import name 'write_pdf_report'`

- [ ] **Step 3: 实现 `write_pdf_report` + `write_summary`**

Append to `eval/report.py`:

```python
SUMMARY_COLUMNS = [
    "pdf", "lang",
    "pages_total", "pages_scanned", "pages_table_missing", "pages_missing",
    "text_P", "text_R", "text_F1", "text_edit_ratio",
    "text_P_word", "text_R_word", "text_F1_word",
    "table_P", "table_R", "table_F1",
    "tables_total", "tables_matched",
]

PER_PAGE_COLUMNS = [
    "page", "status",
    "baseline_text_len", "md_text_len",
    "text_P", "text_R", "text_F1", "text_edit_ratio",
    "baseline_tables", "md_tables",
    "table_P", "table_R", "table_F1",
    "md_method",
]


def _fmt(v: Any) -> str:
    if v is None:
        return ""
    if isinstance(v, float):
        return f"{v:.4f}"
    return str(v)


def write_pdf_report(result: dict[str, Any], output_dir: Path) -> None:
    """写单份 PDF 的 per_page.csv + raw.json 到 output_dir/<name>/"""
    sub = Path(output_dir) / result["name"]
    sub.mkdir(parents=True, exist_ok=True)

    with (sub / "per_page.csv").open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=PER_PAGE_COLUMNS)
        writer.writeheader()
        for p in result["pages"]:
            row = {
                "page": p.get("page", ""),
                "status": p.get("status", ""),
                "baseline_text_len": p.get("baseline_text_len", ""),
                "md_text_len": p.get("md_text_len", ""),
                "text_P": _fmt(p["text"].get("precision")) if not p["text"].get("skipped") else "",
                "text_R": _fmt(p["text"].get("recall")) if not p["text"].get("skipped") else "",
                "text_F1": _fmt(p["text"].get("f1")) if not p["text"].get("skipped") else "",
                "text_edit_ratio": _fmt(p.get("text_edit_ratio")),
                "baseline_tables": p.get("baseline_tables", ""),
                "md_tables": p.get("md_tables", ""),
                "table_P": _fmt(p["table"].get("precision")) if not p["table"].get("skipped") else "",
                "table_R": _fmt(p["table"].get("recall")) if not p["table"].get("skipped") else "",
                "table_F1": _fmt(p["table"].get("f1")) if not p["table"].get("skipped") else "",
                "md_method": p.get("md_method", ""),
            }
            writer.writerow(row)

    with (sub / "raw.json").open("w", encoding="utf-8") as f:
        json.dump(result, f, ensure_ascii=False, indent=2, default=str)


def _tldr(results: list[dict[str, Any]]) -> str:
    if not results:
        return "(no results)"
    total_pages = sum(r["agg"]["pages_total"] for r in results)
    scanned = sum(r["agg"]["pages_scanned"] for r in results)
    # 总体 micro：聚合所有 pages 的 TP/FP/FN
    all_pages = [p for r in results for p in r["pages"]]
    text_P = text_R = text_F1 = 0.0
    if all_pages:
        tp = sum(p.get("_text_tp", 0) for p in all_pages if not p["text"].get("skipped"))
        fp = sum(p.get("_text_fp", 0) for p in all_pages if not p["text"].get("skipped"))
        fn = sum(p.get("_text_fn", 0) for p in all_pages if not p["text"].get("skipped"))
        text_P = tp / (tp + fp) if (tp + fp) > 0 else 0.0
        text_R = tp / (tp + fn) if (tp + fn) > 0 else 0.0
        text_F1 = 2 * text_P * text_R / (text_P + text_R) if (text_P + text_R) > 0 else 0.0
        tp_t = sum(p["table"].get("tp", 0) for p in all_pages if not p["table"].get("skipped"))
        fp_t = sum(p["table"].get("fp", 0) for p in all_pages if not p["table"].get("skipped"))
        fn_t = sum(p["table"].get("fn", 0) for p in all_pages if not p["table"].get("skipped"))
        table_P = tp_t / (tp_t + fp_t) if (tp_t + fp_t) > 0 else 0.0
        table_R = tp_t / (tp_t + fn_t) if (tp_t + fn_t) > 0 else 0.0
        table_F1 = 2 * table_P * table_R / (table_P + table_R) if (table_P + table_R) > 0 else 0.0
    else:
        table_F1 = 0.0

    worst = min(results, key=lambda r: r["agg"]["table_F1"])
    return (
        f"**TL;DR**: {len(results)} 份共 {total_pages} 页，其中 {scanned} 页扫描件无法评估。"
        f" 总体 text_F1={text_F1:.3f}，table_F1={table_F1:.3f}。"
        f" 最弱表格：{worst['name']}（table_F1={worst['agg']['table_F1']:.3f}）。\n\n"
        f"> 注意：所有指标衡量的是 MD 相对 PyMuPDF/pdfplumber 基线的一致性，"
        f"**不是** MD 相对真实数据的准确率。\n"
    )


def write_summary(results: list[dict[str, Any]], output_dir: Path) -> None:
    """写 summary.csv + summary.md"""
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    with (output_dir / "summary.csv").open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=SUMMARY_COLUMNS)
        writer.writeheader()
        for r in results:
            agg = r["agg"]
            row = {"pdf": r["name"]}
            for col in SUMMARY_COLUMNS[1:]:
                row[col] = _fmt(agg.get(col))
            writer.writerow(row)

    with (output_dir / "summary.md").open("w", encoding="utf-8") as f:
        f.write("# pdf2md 评估结果\n\n")
        f.write(_tldr(results))
        f.write("\n## 汇总表\n\n")
        f.write("| " + " | ".join(SUMMARY_COLUMNS) + " |\n")
        f.write("|" + "|".join(["---"] * len(SUMMARY_COLUMNS)) + "|\n")
        for r in results:
            agg = r["agg"]
            row_values = [r["name"]] + [_fmt(agg.get(c)) for c in SUMMARY_COLUMNS[1:]]
            f.write("| " + " | ".join(row_values) + " |\n")
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_output.py -v`
Expected: 2 passed

- [ ] **Step 5: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/report.py eval/tests/test_report_output.py
git commit -m "feat(eval): write summary.csv/md + per_page.csv + raw.json"
```

---

## Task 14: report.py — bad_cases.md with diffs

**Files:**
- Modify: `eval/report.py`
- Create: `eval/tests/test_report_badcases.py`

- [ ] **Step 1: 写失败测试**

Create `eval/tests/test_report_badcases.py`:

```python
from pathlib import Path

from eval.report import write_bad_cases


def _page(num, status, text_f1, table_f1, md_method="native"):
    return {
        "page": num,
        "status": status,
        "baseline_text_len": 100,
        "md_text_len": 100,
        "text": {"precision": text_f1, "recall": text_f1, "f1": text_f1, "skipped": status == "scanned"},
        "text_edit_ratio": 0.1,
        "baseline_tables": 1,
        "md_tables": 1,
        "table": {"precision": table_f1, "recall": table_f1, "f1": table_f1, "skipped": table_f1 is None},
        "md_method": md_method,
        "_baseline_text": "hello world this is the original baseline text for page " + str(num),
        "_md_text": "hallo wrld ths is the original baseline txt for page " + str(num),
        "_baseline_tables": [[["a", "b"], ["1", "2"]]],
        "_md_tables": [[["a", "b"], ["1", "99"]]],
    }


def test_bad_cases_selects_lowest_f1_ok_pages(tmp_path):
    result = {
        "name": "demo",
        "lang": "zh",
        "pages": [
            _page(1, "ok", 0.9, 0.8),
            _page(2, "ok", 0.3, 0.5),  # 最差
            _page(3, "ok", 0.5, 0.4),  # 次差
            _page(4, "ok", 0.7, 0.7),
            _page(5, "scanned", None, None),  # 不选
        ],
    }
    write_bad_cases(result, tmp_path)
    text = (tmp_path / "demo" / "bad_cases.md").read_text(encoding="utf-8")
    # TOP3 应包含 page 2 和 page 3
    assert "Page 2" in text
    assert "Page 3" in text
    # scanned 页不应出现
    assert "Page 5" not in text
    # 含 diff 段落
    assert "Text diff" in text or "text diff" in text.lower()


def test_bad_cases_empty_when_all_pages_skipped(tmp_path):
    result = {
        "name": "demo",
        "lang": "zh",
        "pages": [_page(1, "scanned", None, None)],
    }
    write_bad_cases(result, tmp_path)
    text = (tmp_path / "demo" / "bad_cases.md").read_text(encoding="utf-8")
    assert "无可用 bad case" in text or "no bad case" in text.lower()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_badcases.py -v`
Expected: FAIL with `ImportError: cannot import name 'write_bad_cases'`

- [ ] **Step 3: 实现 `write_bad_cases`**

Append to `eval/report.py`:

```python
BAD_CASE_TOP_N = 3
DIFF_MAX_LINES = 40


def _page_sort_key(p: dict[str, Any]) -> float:
    """排序键：该页存在的 F1 的最小值；两者都没有 → 取极大值（不参选）。"""
    scores = []
    if not p["text"].get("skipped"):
        scores.append(p["text"].get("f1", 1.0))
    if not p["table"].get("skipped"):
        scores.append(p["table"].get("f1", 1.0))
    return min(scores) if scores else 2.0  # 大于任何合法 F1，自然被排到末尾


def _text_diff(baseline: str, md: str) -> str:
    diff = list(unified_diff(
        baseline.splitlines() or [""],
        md.splitlines() or [""],
        fromfile="baseline", tofile="md", lineterm="",
    ))
    if len(diff) > DIFF_MAX_LINES:
        diff = diff[:DIFF_MAX_LINES] + [f"... ({len(diff) - DIFF_MAX_LINES} more lines truncated)"]
    return "\n".join(diff) if diff else "(no diff)"


def _table_diff_md(baseline_tables: list, md_tables: list) -> str:
    if not baseline_tables and not md_tables:
        return "(no tables)"
    base_ms = Counter()
    md_ms = Counter()
    for t in baseline_tables:
        base_ms.update(cell_multiset(t))
    for t in md_tables:
        md_ms.update(cell_multiset(t))
    all_cells = sorted(set(base_ms.keys()) | set(md_ms.keys()))
    lines = ["| cell | in_baseline | in_md | status |", "|---|---|---|---|"]
    for cell in all_cells[:60]:  # 避免单页几千单元格爆炸
        in_b = base_ms.get(cell, 0)
        in_m = md_ms.get(cell, 0)
        if in_b and in_m:
            st = "match"
        elif in_b and not in_m:
            st = "missing_in_md"
        else:
            st = "extra_in_md"
        lines.append(f"| `{cell[:40]}` | {in_b} | {in_m} | {st} |")
    if len(all_cells) > 60:
        lines.append(f"_... {len(all_cells) - 60} more cells truncated_")
    return "\n".join(lines)


def write_bad_cases(result: dict[str, Any], output_dir: Path) -> None:
    """挑 TOP-N 最差 ok 页，写 bad_cases.md"""
    from collections import Counter  # local import to ensure available; Counter already imported at top would also work

    sub = Path(output_dir) / result["name"]
    sub.mkdir(parents=True, exist_ok=True)

    ok_pages = [p for p in result["pages"] if p["status"] == "ok"]
    ok_pages.sort(key=_page_sort_key)
    top = ok_pages[:BAD_CASE_TOP_N]

    out = [f"# Bad cases — {result['name']}\n"]
    if not top:
        out.append("无可用 bad case（所有页被跳过或全绿）。\n")
    else:
        for p in top:
            text_f1 = p["text"].get("f1")
            table_f1 = p["table"].get("f1")
            out.append(
                f"## Page {p['page']} — "
                f"text_F1={_fmt(text_f1) or 'skip'}  "
                f"table_F1={_fmt(table_f1) or 'skip'}  "
                f"method={p.get('md_method', '')}\n"
            )
            out.append("### Text diff (baseline → md)\n```diff\n")
            out.append(_text_diff(p.get("_baseline_text", ""), p.get("_md_text", "")))
            out.append("\n```\n")
            out.append("### Table diff\n")
            out.append(_table_diff_md(p.get("_baseline_tables", []), p.get("_md_tables", [])))
            out.append("\n")

    (sub / "bad_cases.md").write_text("\n".join(out), encoding="utf-8")
```

**重要：** 把文件顶部的 `from collections import Counter` 加进 `eval/report.py` 的 import 区（如果还没有）。检查现有 imports，`Counter` 应在 `typing import Any` 后新增一行：

```python
from collections import Counter
```

Remove the `from collections import Counter` local import inside `write_bad_cases` after ensuring it's at module top.

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_report_badcases.py -v`
Expected: 2 passed

- [ ] **Step 5: 跑全套测试确认无回归**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/ -v`
Expected: 全部 passed。

- [ ] **Step 6: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/report.py eval/tests/test_report_badcases.py
git commit -m "feat(eval): write bad_cases.md with text and table diffs"
```

---

## Task 15: run_eval.py — config 加载、orchestration、控制台

**Files:**
- Create: `eval/run_eval.py`
- Create: `eval/config.yaml`（本地，不入库，内容与 `config.yaml.example` 一致）
- Create: `eval/tests/test_run_eval_smoke.py`

- [ ] **Step 1: 本地拷贝 config**

Run:

```bash
cd /Users/aisenyc/Desktop/pdf-parser && cp eval/config.yaml.example eval/config.yaml
```

- [ ] **Step 2: 写端到端 smoke 测试**

Create `eval/tests/test_run_eval_smoke.py`:

```python
"""端到端冒烟：只跑 1 份最短的 PDF（昆山峰瑞 11 页），验证 4 种产物非空。

如果 config.yaml 不存在或对应 PDF/md_dir 缺失，跳过（开发环境/CI 可能没有）。
"""
from pathlib import Path

import pytest
import yaml

from eval.run_eval import run_evaluation

CONFIG_PATH = Path(__file__).resolve().parents[2] / "eval" / "config.yaml"


def _find_kunshan_item():
    if not CONFIG_PATH.exists():
        return None
    cfg = yaml.safe_load(CONFIG_PATH.read_text(encoding="utf-8"))
    for it in cfg.get("items", []):
        if "昆山峰瑞" in it["name"]:
            if Path(it["pdf"]).exists() and Path(it["md_dir"]).exists():
                return it
    return None


@pytest.mark.skipif(_find_kunshan_item() is None, reason="config.yaml or kunshan PDF not available")
def test_smoke_kunshan(tmp_path):
    item = _find_kunshan_item()
    results = run_evaluation(items=[item], output_dir=tmp_path)
    assert len(results) == 1
    assert (tmp_path / "summary.csv").exists()
    assert (tmp_path / "summary.md").exists()
    sub = tmp_path / item["name"]
    assert (sub / "per_page.csv").exists()
    assert (sub / "raw.json").exists()
    assert (sub / "bad_cases.md").exists()
    # summary.csv 有 1 行
    import csv
    rows = list(csv.DictReader((tmp_path / "summary.csv").open(encoding="utf-8")))
    assert len(rows) == 1
    assert rows[0]["pdf"] == item["name"]
```

- [ ] **Step 3: 运行 smoke 测试确认失败**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_run_eval_smoke.py -v`
Expected: FAIL with `ImportError: cannot import name 'run_evaluation'`

- [ ] **Step 4: 实现 `run_eval.py`**

Create `eval/run_eval.py`:

```python
"""pdf2md 转换质量评估入口。

用法：
    python -m eval.run_eval [--config eval/config.yaml]

从 config.yaml 读取 5 份 PDF，逐份跑评估，产物写到 output_dir。
"""

from __future__ import annotations

import argparse
import json
import logging
import sys
import traceback
from pathlib import Path
from typing import Any

import yaml

from eval.baseline import extract_baseline_tables, extract_baseline_text
from eval.md_parser import parse_page
from eval.report import (
    aggregate_pdf,
    evaluate_page,
    write_bad_cases,
    write_pdf_report,
    write_summary,
)

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s",
)
logger = logging.getLogger("eval")


def _load_manifest(md_dir: Path) -> dict[str, Any]:
    mpath = md_dir / "manifest.json"
    if not mpath.exists():
        return {}
    try:
        return json.loads(mpath.read_text(encoding="utf-8"))
    except Exception as e:  # noqa: BLE001
        logger.warning("manifest.json unreadable in %s: %s", md_dir, e)
        return {}


def _method_for_page(manifest: dict[str, Any], page_num: int) -> str:
    for p in manifest.get("pages", []):
        if p.get("page_number") == page_num:
            return p.get("method", "")
    return ""


def evaluate_pdf(item: dict[str, Any]) -> dict[str, Any]:
    """评估一份 PDF，返回 {name, lang, agg, pages}。"""
    name = item["name"]
    pdf = Path(item["pdf"])
    md_dir = Path(item["md_dir"])
    lang = item.get("lang", "zh")
    logger.info("Evaluating %s (lang=%s)", name, lang)

    baseline_texts = extract_baseline_text(pdf)
    baseline_tables = extract_baseline_tables(pdf)
    total_pages = max(len(baseline_texts), len(baseline_tables))

    manifest = _load_manifest(md_dir)
    pages: list[dict[str, Any]] = []
    for i in range(total_pages):
        page_num = i + 1
        b_text = baseline_texts[i] if i < len(baseline_texts) else ""
        b_tables = baseline_tables[i] if i < len(baseline_tables) else []
        parsed = parse_page(md_dir, page_num)
        if parsed is None:
            pages.append({
                "page": page_num,
                "status": "page_missing",
                "baseline_text_len": len(b_text),
                "md_text_len": 0,
                "text": {"skipped": True, "reason": "page_missing"},
                "text_edit_ratio": None,
                "baseline_tables": len(b_tables),
                "md_tables": 0,
                "table": {"skipped": True, "reason": "page_missing"},
                "md_method": _method_for_page(manifest, page_num),
            })
            continue
        md_text, md_tables = parsed
        page_result = evaluate_page(
            baseline_text=b_text,
            md_text=md_text,
            baseline_tables=b_tables,
            md_tables=md_tables,
            lang=lang,
        )
        page_result["page"] = page_num
        page_result["md_method"] = _method_for_page(manifest, page_num)
        page_result["_baseline_text"] = b_text
        page_result["_md_text"] = md_text
        page_result["_baseline_tables"] = b_tables
        page_result["_md_tables"] = md_tables
        pages.append(page_result)

    agg = aggregate_pdf(pages, lang=lang)
    logger.info(
        "  %s: pages=%d scanned=%d text_F1=%.3f table_F1=%.3f",
        name, agg["pages_total"], agg["pages_scanned"], agg["text_F1"], agg["table_F1"],
    )
    return {"name": name, "lang": lang, "agg": agg, "pages": pages}


def run_evaluation(items: list[dict[str, Any]], output_dir: Path) -> list[dict[str, Any]]:
    """主流程：逐份评估，产出所有报告文件。某份挂掉不影响其他。"""
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)
    results: list[dict[str, Any]] = []
    for item in items:
        try:
            result = evaluate_pdf(item)
            write_pdf_report(result, output_dir)
            write_bad_cases(result, output_dir)
            results.append(result)
        except Exception as e:  # noqa: BLE001
            logger.error("FAILED %s: %s", item.get("name"), e)
            logger.error(traceback.format_exc())
            # 占位结果，summary 里也能看到
            results.append({
                "name": item.get("name", "unknown"),
                "lang": item.get("lang", "zh"),
                "agg": {
                    "lang": item.get("lang", "zh"),
                    "pages_total": 0, "pages_scanned": 0,
                    "pages_table_missing": 0, "pages_missing": 0,
                    "text_P": 0.0, "text_R": 0.0, "text_F1": 0.0,
                    "text_edit_ratio": 0.0,
                    "table_P": 0.0, "table_R": 0.0, "table_F1": 0.0,
                    "tables_total": 0, "tables_matched": 0,
                    "text_P_word": None, "text_R_word": None, "text_F1_word": None,
                    "_failed": str(e),
                },
                "pages": [],
            })
    write_summary(results, output_dir)
    return results


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description="pdf2md 转换质量评估")
    parser.add_argument("--config", default="eval/config.yaml")
    args = parser.parse_args(argv)

    cfg_path = Path(args.config)
    if not cfg_path.exists():
        logger.error("Config not found: %s", cfg_path)
        return 2
    cfg = yaml.safe_load(cfg_path.read_text(encoding="utf-8"))
    items = cfg.get("items", [])
    output_dir = Path(cfg.get("output_dir", "eval_output"))
    # 早期检查：所有 pdf/md_dir 存在
    missing = []
    for it in items:
        if not Path(it["pdf"]).exists():
            missing.append(f"PDF missing: {it['pdf']}")
        if not Path(it["md_dir"]).exists():
            missing.append(f"md_dir missing: {it['md_dir']}")
    if missing:
        logger.error("Fail-fast: config references missing paths:\n%s", "\n".join(missing))
        return 3

    run_evaluation(items, output_dir)
    logger.info("Done. Reports at %s", output_dir.resolve())
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 5: 运行 smoke 测试确认通过**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_run_eval_smoke.py -v`
Expected: 1 passed（或 skipped 如果 config 不可用——此情况下必须手动跑一次 step 6 后再确认）。

- [ ] **Step 6: 跑全套测试确认无回归**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/ -v`
Expected: 全部 passed。

- [ ] **Step 7: Commit**

```bash
cd /Users/aisenyc/Desktop/pdf-parser
git add eval/run_eval.py eval/tests/test_run_eval_smoke.py
git commit -m "feat(eval): add run_eval entry point and end-to-end smoke test"
```

---

## Task 16: 端到端跑 5 份 PDF + 人眼抽查

**Files:** 无代码改动；产物写到 `eval_output/`（被 `.gitignore` 忽略）。

- [ ] **Step 1: 跑完整评估**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && python -m eval.run_eval`
Expected:
- 控制台打印 5 份 PDF 各自的 `pages / scanned / text_F1 / table_F1`
- 结束打印 `Done. Reports at .../eval_output`
- 无 traceback；若某份 FAILED 也应继续跑其他 4 份

- [ ] **Step 2: 查看汇总**

Run:

```bash
cat /Users/aisenyc/Desktop/pdf-parser/eval_output/summary.md
```

Expected: TL;DR + 5 行汇总表，每份 PDF 一行，数字合理（text_F1 通常 >0.5，table_F1 取决于具体 PDF）。

- [ ] **Step 3: 随机抽查 1 份 bad_cases**

Run:

```bash
cat "/Users/aisenyc/Desktop/pdf-parser/eval_output/昆山峰瑞/bad_cases.md" | head -100
```

Expected:
- 标题为 "Bad cases — 昆山峰瑞"
- 有 TOP-3 页段落（除非所有页都是 scanned）
- 每个段落有 `### Text diff` 和 `### Table diff`
- diff 内容能肉眼识别出真实差异（比如 OCR 漏字、表格错列），不是全空或全乱

- [ ] **Step 4: 检查 per_page.csv**

Run:

```bash
head -5 "/Users/aisenyc/Desktop/pdf-parser/eval_output/昆山峰瑞/per_page.csv"
```

Expected: CSV 头 + 前几行数据；`md_method` 列有 `native` / `local_ocr` 等值（不应全空）。

- [ ] **Step 5: 校准性人眼检查**

打开一份 `bad_cases.md` 里 TOP-1 差页，对照源 PDF 看一眼：

- 如果 diff 指出的错误**真的存在** → 评估器工作正常
- 如果 diff 指出"错误"但 PDF 原文就是那样（比如基线抽错但 MD 抽对了）→ 记录下来，后续可能需要调 baseline 策略。**本轮不改**，只做记录。

在本任务下新建 `eval_output/CALIBRATION_NOTES.md`（临时记录，不入库），内容示例：

```markdown
# 校准备忘（人眼抽查，2026-04-17）

## 昆山峰瑞 page X
- 评估报告 text_F1=0.5
- 人眼看：PDF 原文是 XXX，MD 是 YYY，评估器标出的差异属实。✓

## 天津真格 page Y
- 评估报告 table_F1=0.3
- 人眼看：pdfplumber 把表头多抽了一行，MD 里没有 → 其实是基线噪音，不是 MD 错。
- 后续：可以考虑在 baseline.py 里加表头去重，本轮不改。
```

- [ ] **Step 6: 如果 smoke 测试之前被 skip，现在再跑一次确认**

Run: `cd /Users/aisenyc/Desktop/pdf-parser && pytest eval/tests/test_run_eval_smoke.py -v`
Expected: 1 passed

- [ ] **Step 7: 不 commit 产物**

`eval_output/` 已在 `.gitignore`，无需 commit。确认：

```bash
cd /Users/aisenyc/Desktop/pdf-parser && git status
```

Expected: "nothing to commit, working tree clean"（如果之前有 CALIBRATION_NOTES.md 写到 eval_output/ 下也会被忽略）。

---

## Self-Review Checklist

Spec 覆盖：
- § 2 范围（5 份 PDF）→ Task 1 config.yaml.example
- § 3 A + C 两条流水线 → Task 3/4 基线、Task 5/6 MD 解析、Task 7/8/9/10 指标
- § 4 目录结构 → Task 1 + 逐任务 creates
- § 5 数据流 → Task 15 orchestration
- § 6 基线抽取 / 扫描识别 / 归一化 → Task 2/3/4
- § 7 LCS + multiset / micro-average → Task 7/9/10/12
- § 8 报告 schema（summary/per_page/bad_cases/raw.json） → Task 13/14
- § 9 错误处理 → Task 3/4（异常吞掉）+ Task 15（fail-fast 路径 + per-PDF 包装）
- § 10 测试策略 → 每个 Task 都有 pytest
- § 11 开发顺序 → Task 1..16 按 spec 顺序

无 placeholder、类型一致（`Table = list[list[str]]`、`dict[str, Any]`、`Path` 统一）。

---

## Notes on Commits

每个 Task 的 commit 是**实现 commit**（即"为这个评估工具落代码"），不是 spec 的 commit。spec 本身（`docs/superpowers/specs/2026-04-17-pdf2md-eval-design.md`）用户明确说"先不 commit"，应保持未提交状态到用户另行指示。
